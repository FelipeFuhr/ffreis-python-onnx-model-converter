FROM onnx-converter/examples-base:local

RUN uv pip install --python /opt/examples-venv/bin/python --editable ".[cli,sklearn,runtime]"

USER appuser:appgroup
ENTRYPOINT ["python3", "examples/sklearn_example.py"]
