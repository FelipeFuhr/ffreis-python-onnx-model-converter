name: Commit Checks (pre-commit + uv)

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  commit-checks:
<<<<<<< HEAD
    if: ${{ !github.event.pull_request.draft }}
=======
>>>>>>> 0951930157686cb598bc4cd001d294323cf6f1c4
    uses: FelipeFuhr/ffreis-workflows-python/.github/workflows/python-test.yml@v1.0.1
    with:
      python-version: "3.13"
      uv-extras: "dev cli"
      test-dir: "tests/unit_tests"
